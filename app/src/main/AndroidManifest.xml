<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.matthewcorbett.constraintlayout11">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BarrierActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CircularConstraintActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".DimensionConstraintActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PlaceholderActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".PlaceholderRotationActivity" />
        <activity android:name=".GroupActivity" />
    </application>
</manifest>
